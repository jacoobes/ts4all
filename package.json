{
  "name": "ts4all",
  "packageManager": "yarn@3.5.1",
  "gypfile": true,
  "scripts": {
    "build:dev": "node precompile.mjs && node-gyp configure build -m --debug",
    "build:release": "node precompile.mjs && node-gyp configure build -m --release"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^6.1.0"
  }
}
